//Objetos são estruturas que possuem chave e valor
// em que a chave é o nome da propriedade e o valor é o que está armazenado na propriedade.
//O primeiro nome é a chave e o segundo o valor ex:

const objetoPessoa = {
    nome: "Pedro",
    idade: 20,
    altura: 1.80,
};
console.log(objetoPessoa);